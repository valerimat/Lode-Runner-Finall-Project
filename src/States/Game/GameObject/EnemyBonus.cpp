#include "EnemyBonus.h"

//=============================================================================
void EnemyBonus::Surprise(Map* map)
{

	map->add_enemy();
}

	//sf::Vector2f location = { 5,5 };
	//map->get_dynamic().push_back(std::make_unique<Enemy>(ENEMY, location, map->get_textures()[ENEMY_TEXTURE].get()));
}
//=============================================================================

